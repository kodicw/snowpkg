{
  enable = true;
  inlayHints = true;
  servers = {
    nushell = {
      enable = true;
    };

    html = {
      enable = true;
    };

    yamlls = {
      enable = true;
    };

    jsonls = {
      enable = true;
    };

    pyright = {
      enable = true;
    };

    ruff = {
      enable = true;
    };

    nixd = {
      enable = true;
    };

    bashls.enable = true;
  };
}
