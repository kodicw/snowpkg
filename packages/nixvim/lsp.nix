{
  enable = true;
  inlayHints = true;
  servers = {
    nushell = {
      enable = true;
    };

    html = {
      enable = true;
    };
    lua_ls = {
      enable = true;
    };

    yamlls = {
      enable = true;
    };

    jsonls = {
      enable = true;
    };

    pyright = {
      enable = true;
    };

    ruff = {
      enable = true;
    };

    nil_ls = {
      enable = true;
    };

    nixd = {
      enable = true;
    };

    systemd_ls = {
      enable = true;
    };

    zls = {
      enable = true;
    };

    bashls.enable = true;
  };
}
