{
  enable = true;
  inlayHints = true;
  servers = {
    nushell = {
      enable = true;
    };

    html = {
      enable = true;
    };

    yamlls = {
      enable = true;
    };

    jsonls = {
      enable = true;
    };

    pyright = {
      enable = true;
    };

    ruff = {
      enable = true;
    };

    nil_ls = {
      enable = true;
    };

    none_ls = {
      enable = true;
    };

    nixd = {
      enable = true;
    };

    systemd_ls = {
      enable = true;
    };

    bashls.enable = true;
  };
}
